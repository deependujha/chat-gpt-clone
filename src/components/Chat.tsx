import React from 'react';

const Chat = () => {
	return (
		<div className="flex flex-col h-screen w-screen bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-500"></div>
	);
};

export default Chat;
